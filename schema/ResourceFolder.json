{
    "name": "ResourceFolder",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Folder name"
      },
      "description": {
        "type": "string",
        "description": "Optional description of the folder's purpose"
      },
      "parent_folder_id": {
        "type": "string",
        "description": "Reference to parent ResourceFolder entity for nested folders (optional)"
      },
      "display_order": {
        "type": "number",
        "default": 0,
        "description": "Order in which this folder should appear"
      }
    },
    "required": [
      "name"
    ]
  }